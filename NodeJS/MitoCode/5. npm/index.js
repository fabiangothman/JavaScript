/*
1. npm init (al final se crea package.json)
2. instalar las librerias que se quieran con el "--save" al final para que lo guarde en el package.json (esto ya no se usa)
        ej:  npm install lodash --save

3. cuando se cambie de servidor debe instalarse todo lo que este en el package.json asi:
        npm install
        npm i
4. Para correr la aplicacion a traves de npm:
        npm start (lee del package.json)
        npm run desarrollo (lee del package.json)


5. Para desinstalar un modulo con el "--save" al final para que lo guarde en el package.json (esto ya no se usa)
        ej:  npm uninstall lodash --save 
*/